zoo = ["lion","tiger","elephant","horse","wolf"]
zoo.pop(2)
zoo.append("cheetah")
zoo.pop(0)
print(f"Zoo is",zoo)
print(f"first 3",zoo[:3])

"""
Add element → append(add in last), insert(we can specify location)
Remove element → remove(by value), pop(by index)
Update → indexing assignment (e.g., list[index] = value)
Sort → sort()
Reverse → reverse()
Search → in, index()
Iterate → for loop
Create quickly → list comprehension

"""